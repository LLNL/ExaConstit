"""Top-level ``workflows`` namespace.

Currently contains :mod:`workflows.optimization` — the NSGA-III
driver built on top of :mod:`workflow_common`. Future entries
(UQ drivers, parameter sweeps, etc.) would live under sibling
subpackages such as ``workflows.sweeps`` or ``workflows.uq``.
"""
